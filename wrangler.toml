name = "apex-training-api"
main = "worker.js"
compatibility_date = "2024-11-01"

# Non-secret var. You CAN also set this via the dashboard or `wrangler secret`.
# Your DB URL is not sensitive on its own (the token is), so a plain var is fine.
[vars]
TURSO_URL = "libsql://fitness-charalampidi-gabriella.aws-us-west-2.turso.io"

# TURSO_TOKEN and APP_KEY are SECRETS — do NOT put them here.
# Set them with:
#   wrangler secret put TURSO_TOKEN
#   wrangler secret put APP_KEY
